version: 0.2

phases:
  install:
    runtime-versions: 
      docker: 18
  build:
    commands:
      - docker build $IMAGE_REPO_NAME .

#post_build:
  #commands:
    #- printf '[{"name":"EventWriterWeb","imageUri":"%s"}]' $AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/$IMAGE_REPO_NAME:$IMAGE_TAG > imagedefinitions.json

artifacts:
  files:
    imagedefinitions.json


